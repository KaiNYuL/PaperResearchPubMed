论文智能获取与总结桌面应用 - 需求文档 & 技术方案
文档说明
本文档包含该桌面应用的完整需求定义与技术实现方案，聚焦 “基于 LangChain Agent 爬取 PubMed 论文并智能整理” 核心场景，明确前端、后端、打包部署全流程的功能与技术规范。
一、需求文档
1. 项目概述
1.1 项目背景
科研人员常需从 PubMed 等学术数据库批量获取特定主题论文的标题、关键字、摘要，并按规则整理成结构化文档，但手动爬取、筛选、整理效率低；现有工具多无智能体驱动的精准检索能力，且缺乏本地化桌面端的便捷性。
1.2 项目目标
开发一款本地化桌面应用，通过 LangChain ReAct 智能体驱动 PubMed 论文爬取，支持用户输入检索需求后自动爬取符合条件的论文（优先关键字、其次标题、最后摘要匹配），智能整理成结构化文档并输出，同时提供友好的前端交互、完善的错误提示、灵活的配置功能，且打包后可跨机即用。
1.3 项目范围
覆盖范围	具体内容
核心能力	PubMed 论文爬取（关键字 / 标题 / 摘要优先级匹配）、智能整理、文档导出
前端交互	输入框、输出展示、错误提示、设置弹窗、界面定制
后端能力	LangChain Agent 驱动、API 接口、环境自动检测 / 安装
打包部署	Flask+Electron 打包，包含运行环境，支持 Windows 系统跨机安装即用
非覆盖范围	多数据库同时爬取（仅 PubMed）、论文全文爬取、Linux/macOS 适配（可扩展）
2. 功能需求
2.1 核心业务功能
功能点	详细需求
精准爬取	1. 仅爬取 PubMed 公开论文数据；
2. 匹配优先级：关键字 > 标题 > 摘要；
3. 支持用户自定义爬取数量、时间范围；
4. 爬取字段：论文标题、关键字、摘要、发表时间、DOI、作者（可选）；
5. 自动过滤无关键字 / 标题 / 摘要的无效论文
智能整理	1. 按 “匹配度” 排序（关键字匹配 > 标题匹配 > 摘要匹配）；
2. 结构化整理：每篇论文包含 “标题、关键字、摘要、匹配维度、发表时间”；
3. 支持整理为 Markdown/Word/TXT 格式
文档输出	1. 前端展示整理后的论文列表（示例 + 完整内容）；
2. 支持本地导出整理后的文档（指定路径、自定义文件名）；
3. 导出文档格式：Markdown（默认）、Word、TXT
2.2 前端功能
模块	详细需求
输入模块	1. 多行文本输入框，支持换行、粘贴；
2. 输入提示：示例文案（如 “2024-2026 年 糖尿病 机器学习 治疗”）；
3. 输入校验：
- 空输入提示：“请输入检索需求，例如‘2024-2026 年 糖尿病 机器学习 治疗’”；
- 格式错误提示：“时间范围格式有误，建议修改为‘YYYY-YYYY’”；
- 无效关键词提示：“未识别到有效关键词，建议补充核心主题词”；
4. 错误修改建议：自动生成修正后的示例文案（如用户输入 “2024 糖尿病”，建议 “2024-2026 年 糖尿病”）
输出模块	1. 分区展示：顶部 “输出示例”（前 2 篇论文）、下方 “完整输出”（可滚动）；
2. 格式展示：结构化排版（标题加粗、关键字标红、摘要普通文本）；
3. 操作按钮：导出文档、清空输出、复制内容
设置模块	1. 弹窗式设置页面（右上角 “设置” 按钮触发）；
2. AI 配置项：
- AI 模型选择（输入框，默认 “gpt-3.5-turbo”）；
- API 地址（输入框，支持自定义）；
- API Key（密码框，可隐藏 / 显示）；
3. 界面定制项：
- 主题颜色选择（下拉框：默认蓝、浅灰、深蓝）；
- 字体大小（下拉框：小 / 中 / 大）；
- 输出区背景色（单选：白色 / 浅米色）；
4. 配置保存：自动保存到本地，重启后生效；
5. 配置重置：一键恢复默认值
交互体验	1. 简洁美观：扁平化设计，无冗余元素；
2. 加载状态：爬取 / 整理时显示加载动画 + 进度提示；
3. 操作反馈：按钮点击有 hover 效果、操作成功 / 失败有 Toast 提示；
4. 响应式：适配不同屏幕分辨率（最小 800×600）
2.3 后端功能
模块	详细需求
API 接口	1. 核心接口：
- /api/crawl_paper：接收检索需求，返回整理后的论文数据；
- /api/export_doc：接收数据 + 格式，返回导出文件；
- /api/save_config：接收前端配置，保存到本地；
- /api/get_config：获取本地保存的配置；
2. 接口规范：JSON 格式，统一响应体（code/message/data）
智能体驱动	1. LangChain ReAct Agent：
- 解析用户输入，提取检索条件（关键词、时间范围、数量）；
- 决策爬取策略（优先级、过滤规则）；
- 调用爬虫工具，处理爬取结果；
- 智能整理数据，生成结构化内容
爬虫模块	1. PubMed 适配：支持 PubMed API / 网页解析（优先 API）；
2. 容错处理：网络异常时重试（最多 3 次），重试失败返回友好提示；
3. 数据清洗：去除特殊字符、空值、重复论文
环境管理	1. 自动检测：启动时检测 Python 依赖、必要库（langchain、requests、pymed 等）；
2. 自动安装：缺失依赖时弹窗提示 “自动安装所需环境”，用户确认后后台安装；
3. 安装失败：提供手动安装指令（复制到剪贴板）
2.4 打包部署
需求点	详细说明
打包方式	基于 Flask+Electron 打包，将前端、后端、运行环境整合为单个安装包 / 可执行文件
环境包含	1. 内置 Python 运行环境（无需用户安装 Python）；
2. 内置所有依赖库（langchain、fastapi、flask、requests、pymed、vue3 等）；
3. 内置 Electron 运行时
跨机使用	1. 安装包可在无 Python/Node.js 环境的 Windows 电脑上安装；
2. 安装后双击快捷方式即可启动，无需配置环境变量；
3. 离线可用（仅爬取 PubMed 需联网）
3. 非功能需求
3.1 性能需求
指标	要求
爬取速度	10 篇论文爬取 + 整理≤10 秒（网络正常）
响应速度	前端操作（点击按钮、输入校验）≤500ms
导出速度	10 篇论文导出文档≤2 秒
3.2 兼容性需求
环境	要求
操作系统	Windows 10/11（64 位）
硬件	内存≥4GB，硬盘≥100MB 可用空间
3.3 易用性需求
要求	说明
操作门槛	无技术背景用户可 5 分钟内上手（输入需求→点击爬取→导出文档）
提示引导	首次启动显示新手引导（3 步：输入示例→设置 API→导出文档）
错误提示	所有错误均为中文友好提示，避免技术术语（如 “网络连接失败” 而非 “ConnectionTimeout”）
3.4 可靠性需求
要求	说明
容错能力	网络中断、API 失效、输入错误时不崩溃，返回友好提示
数据保存	配置、爬取结果自动本地保存，重启后可恢复
日志记录	后台记录关键操作日志（爬取时间、检索条件、导出记录），便于排查问题

架构分层：前端 (Vue3)→Electron→Flask→FastAPI→LangChain Agent→爬虫；
核心流程：输入校验→Agent 解析→爬虫执行→数据整理→前端展示→文档导出；
打包部署：分前端 / 后端打包，然后整合为exe安装包，支持环境自动安装与跨机使用；
容错设计：错误提示、重试机制、日志记录，保证应用稳定性。

二、技术方案
1. 技术选型
前端技术栈
技术	用途	说明
Vue3 + Composition API	前端框架	轻量、易上手，适配桌面端交互
Element Plus	UI 组件库	提供输入框、按钮、弹窗、下拉框等组件，保证界面美观
Axios	网络请求	调用后端 API 接口
Pinia	状态管理	管理前端配置、加载状态、输出数据
localStorage	本地存储	临时保存配置、输出数据（持久化依赖后端）

后端技术栈
技术	用途	说明
FastAPI	核心后端框架	高性能、自动生成 API 文档，适配异步爬取
Flask	中间层框架	衔接 Electron 与 FastAPI，简化打包
LangChain	智能体框架	实现 ReAct Agent，解析需求、调用工具
pymed	PubMed 爬取	专门适配 PubMed 的 Python 库（优先），备用方案：requests+BeautifulSoup 解析网页
python-docx/markdown	文档导出	生成 Word/Markdown/TXT 格式文档
pyyaml	配置管理	本地保存用户配置（API Key、界面设置等）
pipreqs/auto-py-to-exe	环境打包	检测并打包依赖库

